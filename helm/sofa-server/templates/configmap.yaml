apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "sofa-server.fullname" . }}
  labels:
    {{- include "sofa-server.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
