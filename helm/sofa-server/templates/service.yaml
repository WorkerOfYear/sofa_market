apiVersion: v1
kind: Service
metadata:
  name: {{ include "sofa-server.fullname" . }}
  labels:
    {{- include "sofa-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sofa-server.selectorLabels" . | nindent 4 }}
